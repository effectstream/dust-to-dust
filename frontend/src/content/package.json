{
  "name": "game-content",
  "version": "0.1.0",
  "author": "IOG",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.build.json",
    "generate-json": "npx tsx src/generate-json.ts",
    "generate-json:minimal": "npx tsx src/generate-json.ts --minimal"
  },
  "dependencies": {
    "game2-api": "workspace:*",
    "game2-contract": "workspace:*",
    "pino": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^18.18.6",
    "typescript": "^5.2.2"
  }
}
